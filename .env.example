# Local dev: leave VITE_API_URL unset (or commented) so the app uses the Vite `/api` proxy.
# Set VITE_API_DIRECT=true only if you must call the API cross-origin during dev.
# VITE_API_URL=http://localhost:4000/api
VITE_APP_NAME=Clin-Grow Welfare
VITE_ENVIRONMENT=development
